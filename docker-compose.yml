name: slide-to-confirm-bundle
services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slide-to-confirm-bundle-php
    working_dir: /app
    volumes:
      - .:/app
      - composer-cache:/root/.composer/cache
      - coverage-data:/app/coverage
    environment:
      - COMPOSER_ALLOW_SUPERUSER=1
    command: ["tail", "-f", "/dev/null"]
    networks:
      - slide-to-confirm-bundle-network

volumes:
  composer-cache:
    driver: local
  coverage-data:
    driver: local

networks:
  slide-to-confirm-bundle-network:
    driver: bridge
